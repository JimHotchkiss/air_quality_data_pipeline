-- Note: The name convention of these SQL files will help us to identify the order in which we want these sql queries to 
-- Also we have two schemas, Raw and Presentation


-- Create our schemas 
CREATE SCHEMA IF NOT EXISTS 'raw';
CREATE SCHEMA IF NOT EXISTS 'presentation';